{
  "name": "server-api-at-phone",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development nodemon --experimental-json-modules --experimental-specifier-resolution=node app.js",
    "local": "nodemon server.js",
    "build": "babel src --out-dir build",
    "start-babel": "nodemon server.js --exec babel-node",
    "dev-app": "NODE_ENV=development nodemon --experimental-json-modules --experimental-specifier-resolution=node src/app.js",
    "dev-warnings": "NODE_ENV=development nodemon --experimental-json-modules server.js",
    "dev10": "NODE_ENV=development nodemonn server.js",
    "prod": "NODE_ENV=production nodemon server.js",
    "dreamhost": "NODE_ENV=production nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-babel2": "nodemon server.js --exec babel-node --presets es2015"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-wifi": "^2.0.14"
  }
}
